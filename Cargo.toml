[package]
name = "tl-oracle"
version = "0.1.0"
edition = "2021"
rust-version = "1.98.1"
publish = false
description = "Development-only independent reference semantics for TL"
license = "MIT OR Apache-2.0"

[dependencies]
tl-syntax = { path = "../tl-syntax", features = ["alloc"] }
thiserror = "2"

[lints.rust]
unsafe_code = "forbid"
